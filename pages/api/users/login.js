// KODE SEMENTARA UNTUK PENGUJIAN
// Gantikan isi file pages/api/users/login.js dengan ini
export default function handler(req, res) {
  res.status(200).json({ message: "API is temporarily disabled for testing." });
}